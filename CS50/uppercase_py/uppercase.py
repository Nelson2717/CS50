before = input("Before: ")
print(f"After:  {before.upper()}")

# This code prompts the user for a string input and prints the uppercase version of that string.
# The code uses the str.upper() method to convert the string to uppercase.
# The code is a simple demonstration of string manipulation in Python.